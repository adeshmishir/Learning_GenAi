[project]
name = "genai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.14.0",
    "langchain>=1.3.11",
    "langchain-huggingface>=1.2.2",
    "torch>=2.12.1",
    "transformers>=5.13.0",
]
